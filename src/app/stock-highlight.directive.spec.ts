import { StockHighlightDirective } from './stock-highlight.directive';

describe('StockHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new StockHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
